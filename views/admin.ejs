<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GardenRich</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body class="bg-zinc-50 font-sans antialiased text-zinc-900">
    <%- include("partials/header") %>

        <div class="max-w-2xl mx-auto my-16 px-6">
            <a href="/"
                class="inline-flex items-center text-sm font-semibold text-zinc-500 hover:text-green-700 mb-6 transition-colors">
                &larr; Back to Dashboard
            </a>

            <div class="bg-white rounded-3xl shadow-xl shadow-zinc-200/50 border border-zinc-100 overflow-hidden">
                <div class="p-8 border-b border-zinc-50 bg-zinc-50/50">
                    <h2 class="text-3xl font-black tracking-tight text-zinc-900">Add New Product</h2>
                    <p class="text-zinc-500 text-sm mt-1">Fill in the details to list a new item in the GardenRich
                        store.</p>
                </div>

                <form action="/admin/add-product" method="POST" class="p-8 space-y-6">

                    <div>
                        <label class="block text-xs font-bold uppercase tracking-widest text-zinc-500 mb-2">Product
                            Details</label>
                        <input type="text" name="name" placeholder="e.g. Fresh Organic Milk" required
                            class="w-full bg-zinc-50 border border-zinc-200 p-4 rounded-2xl focus:outline-none focus:ring-4 focus:ring-green-500/10 focus:border-green-500 transition-all">
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label
                                class="block text-xs font-bold uppercase tracking-widest text-zinc-500 mb-2">Quantity/Weight</label>
                            <input type="number" name="weightValue" placeholder="e.g. 500" required
                                class="w-full bg-zinc-50 border border-zinc-200 p-4 rounded-2xl focus:outline-none focus:ring-4 focus:ring-green-500/10 focus:border-green-500 transition-all">
                        </div>
                        <div>
                            <label
                                class="block text-xs font-bold uppercase tracking-widest text-zinc-500 mb-2">Unit</label>
                            <select name="unit"
                                class="w-full bg-zinc-50 border border-zinc-200 p-4 rounded-2xl focus:outline-none focus:ring-4 focus:ring-green-500/10 focus:border-green-500 transition-all appearance-none cursor-pointer">
                                <option value="kg">Kilograms (kg)</option>
                                <option value="g">Grams (g)</option>
                                <option value="ml">Milliliters (ml)</option>
                                <option value="l">Liters (l)</option>
                                <option value="pcs">Pieces (pcs)</option>
                            </select>
                        </div>
                    </div>

                    <div>
                        <label class="block text-xs font-bold uppercase tracking-widest text-zinc-500 mb-2">Price
                            (Rs.)</label>
                        <div class="relative">
                            <span class="absolute left-4 top-1/2 -translate-y-1/2 font-bold text-zinc-400">Rs.</span>
                            <input type="number" name="price" placeholder="0.00" required
                                class="w-full bg-zinc-50 border border-zinc-200 p-4 pl-12 rounded-2xl focus:outline-none focus:ring-4 focus:ring-green-500/10 focus:border-green-500 transition-all">
                        </div>
                    </div>

                    <div>
                        <label class="block text-xs font-bold uppercase tracking-widest text-zinc-500 mb-2">Product
                            Image</label>
                        <div class="relative group">
                            <input type="file" id="imageFile" accept="image/*" class="w-full bg-zinc-50 border border-zinc-200 p-4 rounded-2xl focus:outline-none focus:ring-4 focus:ring-green-500/10 focus:border-green-500 transition-all file:mr-4 file:py-2 file:px-4 file:rounded-full file:border-0 file:text-xs file:font-bold file:bg-zinc-900 file:text-white hover:file:bg-green-700 cursor:pointer">
                            <input type="hidden" name="image" id="imageUrl">
                        </div>
                        <p id="uploadStatus" class="mt-2 text-[10px] font-bold uppercase tracking-wider text-zinc-400">
                        </p>
                    </div>

                    <div class="pt-4">
                        <button type="submit"
                            class="w-full bg-zinc-900 text-white py-4 rounded-2xl font-bold text-lg hover:bg-green-700 transition-all shadow-lg shadow-zinc-200 active:scale-[0.98]">
                            List Product
                        </button>
                    </div>
                </form>
            </div>
        </div>
        <script src="/javascript/index.js"></script>
</body>

</html>