<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GardenRich</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body class="bg-zinc-50 font-sans antialiased text-zinc-900">
    <%- include("partials/header") %>

        <div class="max-w-7xl mx-auto py-12 px-6 lg:px-8">
            <div class="flex items-end justify-between mb-10">
                <div>
                    <% if (query) { %>
                        <span class="text-green-600 font-bold tracking-widest text-xs uppercase">Search Results</span>
                        <h1 class="font-extrabold text-4xl mt-1 tracking-tight text-zinc-900">
                            Results for "<%= query %>"
                        </h1>
                        <a href="/"
                            class="text-sm font-bold text-zinc-400 hover:text-green-600 transition-colors inline-flex items-center mt-2">
                            <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path d="M10 19l-7-7m0 0l7-7m-7 7h18" stroke-width="2" stroke-linecap="round"
                                    stroke-linejoin="round" />
                            </svg>
                            Back to all products
                        </a>
                        <% } else { %>
                            <span class="text-green-600 font-bold tracking-widest text-xs uppercase">Curated
                                Collection</span>
                            <h1 class="font-extrabold text-4xl mt-1 tracking-tight text-zinc-900">Best Sellers</h1>
                            <% } %>
                </div>
            </div>

            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-10">
                <% if (products && products.length> 0) { %>
                    <% products.forEach(product=> { %>
                        <div class="product group bg-white rounded-2xl border border-zinc-100 shadow-sm hover:shadow-xl hover:-translate-y-1 transition-all duration-300 overflow-hidden"
                            data-id="<%= product.id %>">

                            <div class="relative aspect-square overflow-hidden bg-zinc-100">
                                <img src="<%= product.image %>"
                                    class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500">

                                <div
                                    class="absolute top-3 left-3 bg-white/90 backdrop-blur-sm px-2 py-1 rounded-md shadow-sm">
                                    <span class="text-[10px] font-bold text-zinc-600 uppercase tracking-wider">Top
                                        Rated</span>
                                </div>
                            </div>

                            <div class="p-5">
                                <div class="mb-4">
                                    <h3 class="text-lg font-bold text-zinc-800 leading-tight mb-1">
                                        <%= product.name %>
                                    </h3>
                                    <p class="text-sm text-zinc-400 font-medium">
                                        <%= product.weight %>
                                    </p>
                                </div>

                                <div class="flex justify-between items-center pt-2 border-t border-zinc-50">
                                    <div>
                                        <span class="text-xs text-zinc-400 block font-bold uppercase">Price</span>
                                        <span class="text-xl font-black text-green-700">Rs. <%= product.price %></span>
                                    </div>

                                    <div class="quantity-controls flex items-center">
                                        <button onclick="showCounter(this)"
                                            class="add-btn bg-zinc-900 hover:bg-green-700 text-white px-5 py-2.5 rounded-xl text-xs font-bold transition-all shadow-lg shadow-zinc-200 active:scale-95">
                                            ADD TO CART
                                        </button>

                                        <div
                                            class="counter-control hidden items-center bg-zinc-900 text-white rounded-xl overflow-hidden shadow-lg shadow-zinc-200 transition-all">
                                            <button onclick="updateQty(this, -1)"
                                                class="px-3 py-2.5 hover:bg-zinc-800 text-sm font-bold transition-colors leading-none">-</button>

                                            <span
                                                class="qty-text px-2 py-2.5 font-bold text-xs w-8 text-center leading-none">1</span>

                                            <button onclick="updateQty(this, 1)"
                                                class="px-3 py-2.5 hover:bg-zinc-800 text-sm font-bold transition-colors leading-none">+</button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <% }) %>
                            <% } else { %>
                                <div
                                    class="col-span-full py-20 text-center bg-white rounded-[4rem] border border-dashed border-zinc-200 shadow-inner">
                                    <div
                                        class="inline-flex items-center justify-center w-24 h-24 bg-green-50 rounded-full mb-8 animate-pulse">
                                        <svg class="w-12 h-12 text-green-600" fill="none" stroke="currentColor"
                                            viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5"
                                                d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                                        </svg>
                                    </div>
                                    <h2 class="text-3xl font-black text-zinc-900 tracking-tight">No results for "<%=
                                            query %>"</h2>
                                    <p class="text-zinc-500 mt-3 max-w-sm mx-auto font-medium">
                                        We couldn't find any plants matching your search. Try a different keyword or
                                        browse our full collection.
                                    </p>
                                    <div class="mt-10 flex gap-4 justify-center">
                                        <a href="/"
                                            class="bg-zinc-900 text-white px-10 py-4 rounded-2xl font-black hover:bg-green-700 transition-all shadow-lg active:scale-95">
                                            Browse All
                                        </a>
                                    </div>
                                </div>
                                <% } %>
            </div>
        </div>
        <script src="/javascript/index.js"></script>
</body>

</html>