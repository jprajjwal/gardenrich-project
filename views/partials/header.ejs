<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GardenRich</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body>
    <div class="sticky top-0 z-50 w-full bg-white shadow-sm">
        <div class="max-w-7xl mx-auto px-4 py-4 flex items-center justify-between">

            <a href="/" class="flex items-center space-x-2">
                <img src="/images/download.jpeg" alt="Logo" class="w-12 h-12 rounded-lg object-cover">
                <h1 class="text-2xl font-bold text-green-700">GardenRich</h1>
            </a>

            <div class="hidden md:flex flex-1 mx-20">
                <input type="text" placeholder="Search for products..."
                    class="w-full px-4 py-2 border border-gray-300 rounded-l-md focus:outline-none focus:ring-2 focus:ring-green-500">
                <button class="bg-green-600 text-white px-4 rounded-r-md hover:bg-green-500">
                    Search
                </button>
            </div>

            <div class="flex items-center space-x-6 relative">

                <% if (user) { %>
                    <div class="relative">

                        <!-- User Button -->
                        <button id="userButton" onclick="toggleUserMenu(event)"
                            class="flex items-center space-x-2 text-gray-700 hover:text-green-600 focus:outline-none">

                            <div
                                class="w-9 h-9 rounded-full bg-green-600 text-white flex items-center justify-center font-bold">
                                <%= user.email.charAt(0).toUpperCase() %>
                            </div>

                            <span class="font-medium">
                                <%= user.name %>
                            </span>

                            <svg class="w-4 h-4 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M19 9l-7 7-7-7" />
                            </svg>
                        </button>

                        <!-- Dropdown -->
                        <div id="userMenu"
                            class="hidden absolute right-0 mt-3 w-48 bg-white border rounded-lg shadow-lg z-50">

                            <a href="/profile" class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100">
                                Profile
                            </a>

                            <% if (user.role==="ADMIN" ) { %>
                                <a href="/admin" class="block px-4 py-2 text-sm text-green-700 hover:bg-gray-100">
                                    Add Product
                                </a>
                                <% } %>

                                    <a href="/logout" class="block px-4 py-2 text-sm text-red-600 hover:bg-gray-100">
                                        Logout
                                    </a>
                        </div>

                    </div>
                    <% } else { %>
                        <a href="/login" class="text-gray-700 font-medium hover:text-green-600">
                            Login
                        </a>
                        <% } %>

                            <!-- Cart -->
                            <a href="/cart" class="relative flex items-center text-gray-700 hover:text-green-600">
                                🛒
                                <span id="cartCount"
                                    class="absolute -top-2 -right-3 bg-green-600 text-white text-xs w-5 h-5 flex items-center justify-center rounded-full">
                                    <%= cartCount %>
                                </span>
                            </a>
            </div>
        </div>
    </div>


    <script src="/javascript/index.js"></script>
</body>

</html>