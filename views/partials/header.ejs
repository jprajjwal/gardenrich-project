<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GardenRich</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body>
    <nav class="sticky top-0 z-50 w-full bg-white/80 backdrop-blur-md border-b border-zinc-100 shadow-sm">
    <div class="max-w-7xl mx-auto px-6 py-3 flex items-center justify-between">

        <a href="/" class="flex items-center space-x-3 group">
            <div class="overflow-hidden rounded-xl shadow-inner border border-zinc-100">
                <img src="/images/download.jpeg" alt="Logo" class="w-10 h-10 object-cover group-hover:scale-110 transition-transform duration-300">
            </div>
            <h1 class="text-2xl font-black text-zinc-900 tracking-tighter">
                Garden<span class="text-green-600">Rich</span>
            </h1>
        </a>

        <div class="hidden md:flex flex-1 mx-12 max-w-2xl">
            <div class="relative w-full group">
                <input type="text" placeholder="Search for fresh plants..."
                    class="w-full pl-12 pr-4 py-2.5 bg-zinc-50 border border-zinc-200 rounded-2xl focus:outline-none focus:ring-4 focus:ring-green-500/10 focus:border-green-500 transition-all text-sm">
                <div class="absolute left-4 top-1/2 -translate-y-1/2 text-zinc-400 group-focus-within:text-green-600 transition-colors">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                    </svg>
                </div>
            </div>
        </div>

        <div class="flex items-center space-x-2">

            <% if (user) { %>
                <div class="relative">
                    <button id="userButton" onclick="toggleUserMenu(event)"
                        class="flex items-center space-x-3 text-zinc-700 hover:bg-zinc-50 p-1.5 pr-4 rounded-full border border-zinc-100 transition-all hover:shadow-md active:scale-95">
                        
                        <div class="w-8 h-8 rounded-full bg-green-600 text-white flex items-center justify-center font-bold text-sm shadow-sm ring-2 ring-white">
                            <%= user.name.charAt(0).toUpperCase() %>
                        </div>

                        <span class="font-bold text-sm tracking-tight text-zinc-800">
                            <%= user.name %>
                        </span>

                        <svg class="w-4 h-4 text-zinc-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                        </svg>
                    </button>

                    <div id="userMenu"
                        class="hidden absolute right-0 mt-4 w-56 bg-white border border-zinc-100 rounded-2xl shadow-2xl z-50 py-2 ring-1 ring-black/5 animate-in fade-in slide-in-from-top-2 duration-200">
                        <div class="px-4 py-2 mb-2 border-b border-zinc-50">
                            <p class="text-[10px] font-bold text-zinc-400 uppercase tracking-widest">Account</p>
                        </div>
                        <a href="/profile" class="flex items-center px-4 py-2.5 mx-2 text-sm text-zinc-700 font-semibold rounded-xl hover:bg-zinc-50 hover:text-green-700 transition-colors group">
                            <svg class="w-5 h-5 mr-3 text-zinc-400 group-hover:text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                            </svg>
                            Profile
                        </a>

                        <% if (user.role === "ADMIN") { %>
                            <a href="/admin" class="flex items-center px-4 py-2.5 mx-2 text-sm text-zinc-700 font-semibold rounded-xl hover:bg-zinc-50 hover:text-green-700 transition-colors group">
                                <svg class="w-5 h-5 mr-3 text-zinc-400 group-hover:text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v3m0 0v3m0-3h3m-3 0H9m12 0a9 9 0 11-18 0 9 9 0 0118 0z" />
                                </svg>
                                Dashboard
                            </a>
                        <% } %>

                        <div class="mt-2 pt-2 border-t border-zinc-50">
                            <a href="/logout" class="flex items-center px-4 py-2.5 mx-2 text-sm text-red-500 font-bold rounded-xl hover:bg-red-50 transition-colors">
                                <svg class="w-5 h-5 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" />
                                </svg>
                                Sign Out
                            </a>
                        </div>
                    </div>
                </div>
            <% } else { %>
                <a href="/login" class="text-sm font-bold text-zinc-800 hover:text-green-700 px-4 py-2 transition-colors">
                    Login
                </a>
                <a href="/register" class="bg-zinc-900 text-white text-sm font-bold px-5 py-2 rounded-full hover:bg-green-700 transition-all shadow-lg shadow-zinc-200">
                    Get Started
                </a>
            <% } %>

            <a href="/cart" class="relative p-2 text-zinc-700 hover:bg-zinc-50 rounded-full transition-all group">
                <svg class="w-6 h-6 group-hover:text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z" />
                </svg>
                <% if (cartCount > 0) { %>
                    <span id="cartCount" class="absolute top-1 right-1 bg-green-600 text-white text-[10px] font-black w-4 h-4 flex items-center justify-center rounded-full ring-2 ring-white">
                        <%= cartCount %>
                    </span>
                <% } %>
            </a>
        </div>
    </div>
</nav>

<script src="/javascript/index.js"></script>
</body>

</html>